management:
  endpoint:
    gateway:
      enabled: true

# server.port 8080으로 통일
server:
  port: 8080

spring:
  application:
    name: gateway-service
  config:
    import:
      - optional:classpath:application-common.yaml
      - optional:classpath:application-auth-prod.yaml
  cloud:
    gateway:
      # 4.0.x 버전 Servlet Gateway의 핵심 설정 프리픽스
      server:
        webmvc:
          actuator:
            enabled: true
          cors: # cors 허용
            configurations:
              '[/**]':
                allowed-origins: [ "https://ta.greenart.n-e.kr" ]
                allowed-methods: [ "*" ]
                allowed-headers: [ "*" ]
                allow-credentials: true
                max-age: 3600

          # 라우팅 설정
          routes:
            - id: auth-service
              uri: http://auth-service:80
              predicates:
                - Path=/api/user/**

            - id: store-service
              uri: http://store-service:80
              predicates:
                - Path=/api/store/**

            - id: order-service
              uri: http://order-service:80
              predicates:
                - Path=/api/order, /api/order/**



