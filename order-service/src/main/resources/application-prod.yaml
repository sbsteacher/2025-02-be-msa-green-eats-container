constants:
  api:
    prefix: /api/order

# server.port 8080으로 통일
server:
  port: 8080

spring:
  application:
    name: order-service
  config:
    import:
      - optional:classpath:application-common.yaml
      - optional:classpath:application-auth-prod.yaml
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:log4jdbc:mysql://112.222.157.157:5011/eats_order?createDatabaseIfNotExist=true&characterEncoding=UTF-8
    username: green1
    password: spring502
  jpa:
    hibernate:
      ddl-auto: none
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"

